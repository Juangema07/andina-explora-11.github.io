/* Compatibilidad: la música del proyecto se gestiona en andina-pro.js.
   Este archivo se conserva para no romper enlaces antiguos, pero no inicializa
   componentes ni busca elementos que no existan en la página actual. */

name: Aplicar mejora integral Andina
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "El rediseño actual se mantiene directamente en main; no se inyectan scripts antiguos."
